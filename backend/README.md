# Backend

This folder will contain the Flask API for the AI product workflow.

## Planned structure

- `app.py` — Flask application entry point
- `routes/` — API route definitions
- `services/` — Business logic and integration with crewai_workflow
